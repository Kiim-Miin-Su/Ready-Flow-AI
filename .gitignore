# envs / caches
.venv/
__pycache__/
*.pyc
.DS_Store
.vercel/
.ipynb_checkpoints/

# raw source data (large; not needed at serve time)
data/

# heavy build intermediates — regenerated by build_dataset.py / train.py
build/*.parquet
build/*.csv

# DO commit the small serving artifacts (needed by Vercel includeFiles):
!build/model.pkl
!build/serve_tables.json
!build/metrics.json
