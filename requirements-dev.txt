# Local training / preprocessing deps (NOT deployed to Vercel).
-r requirements.txt
pandas==3.0.3
pyarrow==24.0.0
scikit-learn==1.9.0
optuna==4.9.0
uvicorn==0.34.0
httpx==0.28.1          # for TestClient (CI smoke)
